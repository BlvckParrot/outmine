# Development only. The browser talks to the Bun API server directly, so no Vite proxy
# is involved. Production leaves this unset and everything stays same-origin.
VITE_API_ORIGIN=http://localhost:3000
